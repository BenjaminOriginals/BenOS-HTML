# BenOS-HTML
The HTML edition of BenOS! Works very well, and acts like an OS. Has multiple useful apps, and was built by DedeProGames.
The HTML file can be loaded as long as you have a browser that can load HTML or a computer that can open HTML files natively.
